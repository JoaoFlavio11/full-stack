// permite executar diferentes ações com base em condições

// ternário: cond ? "op1 verdadeiro" : "op2 falso"

// falsy -> 0, "", null, NaN
// truthy -> verdadeiro em contexto bool obrigatório

// if (se) / else (se nao) / esleif (outra opção)

// switch: analisa caso a caso da condição -> case: break | default;

// exceções: try catch finally